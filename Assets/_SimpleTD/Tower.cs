using UnityEngine;
using System.Collections;

/// <summary>
/// Handles visual and data changes on every turret.
/// Assign script to build position.
/// </summary>
[System.Obsolete("not used anymore")]
public class Tower :MonoBehaviour {

    //public UpgradeTree activeUpgrade;

    void InitUpgrade() {
        //GameObject go = GameObject.Instantiate(activeUpgrade.upgradePrefabWithTree, transform.position, new Quaternion()) as GameObject;
        //go.transform.parent = transform;
    }
}
